# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.pytest_cache/
.coverage
htmlcov/

# 虚拟环境
.venv/
venv/
env/

# 环境变量与密钥
.env
.env.local
.env.*.local

# 运行时数据
data/
*.db
*.db-journal
*.sqlite
*.sqlite3

# 模型权重（已单独管理，不入库）
# 注意：用前导 / 锚定项目根，避免误伤 app/models/（数据库模型代码目录）
/models/
!/models/.gitkeep

# IDE
.vscode/
.idea/
*.swp
*.swo

# 系统文件
.DS_Store
Thumbs.db

# 日志
*.log
logs/

# 构建产物
dist/
build/
wheels/

# Claude/zcode 配置
.claude/

# 临时测试脚本（_xxx.py，正式测试在 tests/）
test.py
/test/

# 评测数据集（体积大，从 HuggingFace 按需下载）
InduOCRBench/
pdfQA-main/

# 评测中间产物（eval_result*.json / judge_cache 每次重跑会更新）
tests/eval/eval_result.json
tests/eval/eval_result_ocr.json
tests/eval/eval_result_ocr_llmjudge.json
tests/eval/eval_result_ragas.json
tests/eval/judge_cache.jsonl
tests/eval/ragas_gen_cache.jsonl

# 评测用独立 Chroma 库
data/chroma_eval/
data/chroma_eval_ocr/

# MinerU 解析结果缓存（OCR→RAG 端到端评测用）
data/ocr_md_cache/
